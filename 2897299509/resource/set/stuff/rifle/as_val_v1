;//	AS VAL standard + ACOG scope + silencer

{from "pattern rifle"
	{tag "-rifle smg pistol2_shell assaultrifle silenced"}
	{tag "autorifle_sup auto_eject"}
	{entity "as_val"}
	{fireSound	"rifle/silenced_val"}
	{fireSoundClose	"rifle/silenced_val_close"}
	{reloadSound "ak47"}
	
	{filling "ammo 9x39" 20}
	{calibre 9}
	{mass 2.5}			;kg unloaded

	{Automatic}
	("fire rate" rpm(800))
	("velocity" mps(280))

 	{speed 280}

	("human health" damage(140))
	("armor health" damage(4))
	("penetration" args 7.5 4.5)
	{range 69 69}
	{burst
		{short 2 1}			; burst shots on long distance, rand (+-)shots
		{long 5 2}			; burst shots on short distance, rand (+-)shots
		{rangeKoef 0.5}		; rangeKoef * aimRange = max long burst distance
	}
	("zoom_asval")
}