; CX40K compatibility wrapper.
; Preserve the complete Code-X vision field registry, then add the LV40K
; infantry field names consumed by SC trooper templates.
(include "vision_fields_codex_compat.inc")

{"human_irregular"
	{uncover 10}
	("h_fov" h(90))
	("v_fov" v(100))
	("vision_human")
}
