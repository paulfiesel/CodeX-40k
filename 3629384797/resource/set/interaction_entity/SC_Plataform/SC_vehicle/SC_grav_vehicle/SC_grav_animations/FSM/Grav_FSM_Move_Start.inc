
;=============================================================================================================================
; DEFINES 
;=============================================================================================================================

; // ...

;=============================================================================================================================
; FSM OVERLOAD
;=============================================================================================================================

(define "SC_FSM_MOVE_START_Define_Enter"
)

(define "SC_FSM_MOVE_START_Define_Event"
	{Ani_play "SC_grav_move_start" callback}
)

(define "SC_FSM_MOVE_START_Define_Exit"
	{Ani_Stop "SC_grav_move_start"}
)

;=============================================================================================================================
; TRANSITIONS/CALLS
;=============================================================================================================================

{on animation_end "SC_grav_move_start"
	{delay 0.1
		("SC_FSM_Define_Update_FSM" do(remove) state(MOVE_START))
		{if tagged "sc_request_tag_is_moving" 	("SC_FSM_Define_Update_FSM" do(add) state(MOVE_LOOP))}
	}
}