	
;=============================================================================================================================
; DEFINES 
;=============================================================================================================================

; // ...

;=============================================================================================================================
; FSM OVERLOAD
;=============================================================================================================================

(define "SC_FSM_RECOIL_Define_Enter"
	{Kill_delay "sc_fsm_recoil_delay_reset_state"}
)

(define "SC_FSM_RECOIL_Define_Event"
	{Call "SC_FSM_RECOIL_Call_Play_Animations"}
)

(define "SC_FSM_RECOIL_Define_Exit"
	{Ani_Stop "recoil1"}
)

;=============================================================================================================================
; TRANSITIONS/CALLS
;=============================================================================================================================

{on animation_end "recoil1"	
	
	; // WILL CRASH WITHOUT THE DELAY!
	{delay 0.01 "sc_fsm_recoil_delay_reset_state"
		("SC_FSM_Define_Update_FSM" do(remove) state(RECOIL))
	}
}


