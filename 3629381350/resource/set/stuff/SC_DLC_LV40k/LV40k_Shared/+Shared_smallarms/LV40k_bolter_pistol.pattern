{from "pattern bullet"
	{cursor "ironsights/rifle"}
	{cursorReloading "carbine_reload"}
	
	("SC_Weapon_Accuracy_LV40k_Boltpistol")
	
	("SC_Weapon_Smallarms"
		weaponCategory(pistol)
		weaponTag(LV40k_weapon lv40k_bolter_pistol)
		animationType(pistol)
		
		invSize(4 2)
		weight(9.6)	
		
		calibre(19.7)
		aimbone(foresight3)
		curve(bullet_bolter) 
		
		reloadSpeed(3.5)
		fireRate(150)	
		projectileSpeed(400)
		
		; see on SC_weapons
		recoilStrenght(High)	
		fireMode(Auto_v2)
		
		armorPiercing(Smallarms_T5)
		healthDamage(900)

		minRange(100)	maxRange(100)
	)
	{parameters "kraken"
		("SC_Weapon_Smallarms"
			weaponCategory(pistol)
			weaponTag(LV40k_weapon lv40k_bolter_pistol)
			animationType(pistol)
			
			invSize(4 2)
			weight(9.6)	
			
			calibre(19.7)
			aimbone(foresight3)
			curve(bullet_bolter) 
			
			reloadSpeed(3.5)
			fireRate(150)	
			projectileSpeed(400)

			; see on SC_weapons
			recoilStrenght(High)	
			fireMode(Auto_v2)
			
			armorPiercing(Smallarms_T8)
			healthDamage(1500)

			minRange(100)	maxRange(100)
		)
	}
}
